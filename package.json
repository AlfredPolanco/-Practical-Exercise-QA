{
  "scripts": {
    "tests": "npx cypress run --browser chrome",
    "format": "prettier --write 'cypress/**/*.js'",
    "check-format": "prettier --check 'cypress/**/*.js'"
  },
  "devDependencies": {
    "@faker-js/faker": "^8.4.1",
    "cypress": "^13.6.6",
    "prettier": "3.2.5"
  }
}
